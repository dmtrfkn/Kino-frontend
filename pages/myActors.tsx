import ProfileLayout from '@/widgets/ProfileLayout';
import React from 'react';

const myActors = () => {
  return (
    <ProfileLayout>
      <div>myActors</div>
    </ProfileLayout>
  );
};

export default myActors;
